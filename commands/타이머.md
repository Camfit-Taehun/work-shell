---
description: 포모도로 타이머 (/ws-timer의 한국어 별칭)
argument-hint: [분] | stop | status
allowed-tools: [Read, Write, Bash(date:*), Bash(osascript:*)]
---

# 타이머 - 포모도로 타이머

이 커맨드는 `/ws-timer`와 동일합니다.

load work-shell-core skill, then follow /ws-timer command workflow.

User argument: $ARGUMENTS
