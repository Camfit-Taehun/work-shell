---
description: 작업 통계 (/ws-stats의 한국어 별칭)
argument-hint: [period]
allowed-tools: [Read, Bash(git:*), Bash(date:*)]
---

# 통계 - 작업 통계

이 커맨드는 `/ws-stats`와 동일합니다.

load work-shell-core skill, then follow /ws-stats command workflow.

User argument: $ARGUMENTS
