---
description: 세션 브랜칭 + 컨텍스트 전달 (/ws-fork의 한국어 별칭)
argument-hint: [name] | list | merge <name> | delete <name>
allowed-tools: [Read, Write, Bash(claude:*), Bash(osascript:*), Bash(mkdir:*), Bash(rm:*), Bash(cp:*), AskUserQuestion]
---

# 포크 - 세션 브랜칭

이 커맨드는 `/ws-fork`와 동일합니다.

load fork-manager skill, then follow /ws-fork command workflow.

User argument: $ARGUMENTS
