---
description: 스마트 스태시 (/ws-stash의 한국어 별칭)
argument-hint: [메시지] | pop | list
allowed-tools: [Read, Write, Bash(git:*), Bash(date:*)]
---

# 임시저장 - 스마트 스태시

이 커맨드는 `/ws-stash`와 동일합니다.

load work-shell-core skill, then follow /ws-stash command workflow.

User argument: $ARGUMENTS
