---
description: Git 브랜치 관리 (/ws-branch의 한국어 별칭)
argument-hint: [브랜치명] [--from <base>]
allowed-tools: [Read, Write, Bash(git:*), AskUserQuestion]
---

# 브랜치 - Git 브랜치 관리

이 커맨드는 `/ws-branch`와 동일합니다.

load work-shell-core skill, then follow /ws-branch command workflow.

User argument: $ARGUMENTS
