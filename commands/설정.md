---
description: work-shell 설정 관리 (/ws-config의 한국어 별칭)
argument-hint: [key] [value] | list | reset
allowed-tools: [Read, Write]
---

# 설정 - work-shell 설정 관리

이 커맨드는 `/ws-config`와 동일합니다.

load work-shell-core skill, then follow /ws-config command workflow.

User argument: $ARGUMENTS
