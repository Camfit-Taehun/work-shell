---
description: 팀 동기화 (/ws-sync의 한국어 별칭)
allowed-tools: [Read, Write, Bash(git:*), Bash(cp:*)]
---

# 동기화 - 팀 동기화

이 커맨드는 `/ws-sync`와 동일합니다.

load work-shell-core skill, then follow /ws-sync command workflow.
